This directory contains the model assets used by the static demo.

- `face_landmarker.task`: MediaPipe Face Landmarker task model.
- `canonical_face_model.obj`: MediaPipe canonical face mesh used by the face-only avatar.

Keep these files in `public/models/` so Vite copies them into `dist/models/` for GitHub Pages.
