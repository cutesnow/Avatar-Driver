This directory is reserved for optional VRM avatar experiments.

The default GitHub Pages demo uses `public/models/canonical_face_model.obj` instead of a VRM avatar because it is easier to inspect landmark-driven face mesh behavior.
